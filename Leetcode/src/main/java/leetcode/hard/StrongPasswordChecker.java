package leetcode.hard;

/**
 * Created by cc on 2017/4/12.
 */
public class StrongPasswordChecker {

    /**
     * loop through s,
     * 1.check if satisfy uppercase, lowercase, digits. if not whats missing,
     * can be positive number
     * 2.count how many three same continuous same letters.
     * negative number
     * 3.how many letters we have.
     * negative number.
     *
     * make the sum to 0, is the result?
     *
     * */
    public int strongPasswordChecker(String s) {
        return 0;
    }

}
